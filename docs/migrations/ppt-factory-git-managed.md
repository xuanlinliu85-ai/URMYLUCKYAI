# PPT Factory Git-Managed Migration Report

Date: 2026-09-13  
Project: `ppt-factory`  
Canonical path: `projects/PPT`  
Migration branch: `codex/migrate-ppt-factory-git-managed`  
Baseline: `d4ba8a86f6baf55d22088eb6c7debb2ace7e940d`

## Source verification

- Source workspace: `<LOCAL_WORKSPACE>`
- Source repository: local Git repository with no configured remote
- Source branch: `master`
- Source commit: `696a0bcf9f82caf9002626abdb79c325e30ea67a`
- Source status: `AGENTS.md` and generated `next-env.d.ts` modified; `output/` and `tmp/` untracked
- Additional worktrees: three clean feature worktrees were found; their commits are already merged into `master`
- Manifest `source_commit`: `null`, because the local source commit is not resolvable in `xuanlinliu85-ai/URMYLUCKYAI`

## Inventory and comparison

- Source Git-tracked files: 321
- Repository snapshot Git-tracked files: 321
- Public-safe comparison candidates: 258 source files and 257 repository files
- Raw-hash identical files: 167
- Line-ending-only differences: 87
- Repository-newer files: 3
- Source-only generated files: 1
- Missing repository source files: 0

The three semantic differences were retained from the repository because they are the V1.1 canonical Research Artifact integration:

- `projects/PPT/package.json`: adds `contract:test`
- `projects/PPT/lib/research-artifact.mjs`: consumes the root canonical validator and preserves structured risks/watchpoints
- `projects/PPT/tests/research-artifact.test.mjs`: uses the canonical fixture and verifies zero artifact mutation

`next-env.d.ts` was not imported because it is generated by Next.js. No PPT business source file required copying.

## Public-safety gate

The migration imported no source-workspace files. The following local-only material remains outside the public repository:

- `.env.local`
- local output and temporary directories
- dependency and build directories
- alternate Git worktrees
- an internal example presentation and its inspection output
- a duplicate specification archive

Tracked text files were scanned for common OpenAI, GitHub, AWS, private-key, and credential-assignment patterns. The only generic assignment matches were synthetic credential-safety test fixtures. No provider credential or private key was found.

## Migration changes

- Updated `projects/SNAPSHOT_MANIFEST.json` for `ppt-factory`
- Set `source` to `github:xuanlinliu85-ai/URMYLUCKYAI`
- Set `canonical` to `true`
- Set `sync_mode` to `git_managed`
- Added this migration report
- Imported business files: 0
- Modified PPT business files: 0

## Tests

- Root `npm ci`: PASS
- Root `npm run build`: PASS
- Root `npm test`: PASS, 11/11
- Root `npm run lint`: PASS with 23 pre-existing warnings and 0 errors
- Root `npm run macro:test:offline`: PASS
- PPT `pnpm install --frozen-lockfile`: PASS
- PPT `pnpm run typecheck`: PASS
- PPT `pnpm run test`: PASS, 165/165
- Root `npm run artifact:validate`: PASS, 4 artifacts
- Root `npm run governance:validate`: PASS, 8 projects and 9 active Skills
- Root `npm run ppt:contract:test`: PASS, 1/1

The first sandboxed PPT test attempt encountered Windows `EPERM` reads against pnpm hard-linked dependency files. Re-running the same commands with normal filesystem access passed without source or lockfile changes.

## Boundaries and remaining risk

- AA20 functional changes: 0
- DeerFlow runtime changes: 0
- Nested Git repositories added: 0
- Submodules or subtrees added: 0
- Research Artifact version: unchanged at `1.0.0`
- Conflict status: none
- Remaining risk: the legacy local workspace remains dirty and is retained as a backup; GitHub becomes canonical after this PR is reviewed and merged
